
使用spring security 和 jwt
登录相关的流程
1.前端输出用户密码传到后端

2.后端判断用户密码是否正确

3.如果用户名密码正确(不正确就重新输入)

4.生成一个jwt令牌,返回给前端

5.前端拿到jwt令牌,会在后面的每一次请求中都携带jwt令牌

6.后端会写一个jwt令牌相关的拦截器

7.每次请求之前,走一遍拦截器,判断用户的jwt令牌是否合法

8.如果不存在就拦截
